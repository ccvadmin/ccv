<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
<t t-name="web.Many2manyImagesField" owl="1">
    <div t-attf-class="oe_fileupload {{props.className ? props.className : ''}}" aria-atomic="true">
        <div class="o_attachments">
            <t t-foreach="files" t-as="file" t-key="file_index">
                <t t-call="Many2manyImagesField.attachment_preview"/>
            </t>
        </div>
        <div t-if="!props.readonly" class="oe_add">
            <FileInput
                acceptedFileExtensions="props.acceptedFileExtensions"
                multiUpload="true"
                onUpload.bind="onFileUploaded"
                resModel="props.record.resModel"
                resId="props.record.data.id or 0"
            >
                <button class="btn btn-secondary o_attach" data-tooltip="Attach">
                    <span class="fa fa-paperclip" aria-label="Attach"/> <t t-esc="props.uploadText"/>
                </button>
            </FileInput>
        </div>
    </div>
    <div id="imageModal" class="modal" tabindex="-1" role="dialog" aria-labelledby="imageModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="imageModalLabel">Ảnh</h5>
                    <button type="button" class="btn btn-danger close" data-dismiss="modal" aria-label="Close"
                        t-on-click="()=>closeImageModal()"
                    >
                        <span aria-hidden="true">&#215;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <img id="modalImage" class="img-fluid" src="" alt="Image Preview"/>
                </div>
            </div>
        </div>
    </div>

    <div id="renameModal" class="modal" tabindex="-1" role="dialog" aria-labelledby="renameModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="btn btn-danger close" data-dismiss="modal" aria-label="Close"
                        t-on-click="() => closeRenameModal()">
                        <span aria-hidden="true">&#215;</span>
                    </button>
                    <h5 class="modal-title" id="renameModalLabel">Đổi tên</h5>
                </div>
                <div class="modal-body">
                    <label for="newFileName">Tên mới:</label>
                    <input type="text" id="newFileName" class="form-control" />
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal" t-on-click="() => closeRenameModal()">Đóng</button>
                    <button type="button" class="btn btn-primary" t-on-click="() => saveRename()">Lưu</button>
                </div>
            </div>
        </div>
    </div>
</t>
<t t-name="Many2manyImagesField.attachment_preview" owl="1">
    <t t-set="editable" t-value="!props.readonly"/>
    <div t-attf-class="o_attachment o_attachment_many2many d-inline-block #{ editable ? 'o_attachment_editable' : '' } #{upload ? 'o_attachment_uploading' : ''}" t-att-title="file.name">
        <div class="o_attachment_wrap">
            <t t-set="ext" t-value="getExtension(file)"/>
            <div class="o_image_box float-start w-100" t-att-data-tooltip="file.name">
                <div>
                    <span class="o_image o_hover w-100" t-att-data-mimetype="file.mimetype" t-att-data-ext="ext" role="img"
                    t-att-style="'background-image: url(' + getUrl(file.id) + ');height: 200px;'"
                    t-on-click="()=>openImageModal(getUrl(file.id),file.name)"
                    />
                </div>
            </div>

            <div class="caption w-100" style="white-space: normal;">
                <a class="ml4" t-att-data-tooltip="file.name" t-att-href="getUrl(file.id)" target="_blank">
                    <t t-esc='file.name'/>
            </a>
            </div>
            <div class="caption small">
                <a class="ml4 small text-uppercase" t-att-href="getUrl(file.id)"><b><t t-esc='ext'/></b></a>

                <a class="btn btn-primary m-2" t-att-href="getUrl(file.id)" download="">
                    <i class="fa fa-download" aria-hidden="true"></i>
                </a>

                <button class="btn btn-warning m-2" t-on-click="() => openRenameModal(file)">
                    <i class="fa fa-pencil" aria-hidden="true"></i> Đổi tên
                </button>

                <div t-if="editable" class="progress o_attachment_progress_bar">
                    <div class="progress-bar progress-bar-striped active" style="width: 100%">Uploading</div>
                </div>
            </div>

            <div class="o_attachment_uploaded"><i class="text-success fa fa-check" role="img" aria-label="Uploaded" title="Uploaded"/></div>
            <div t-if="editable" class="o_attachment_delete" t-on-click.stop="() => this.onFileRemove(file.id)"><span class="text-white" role="img" aria-label="Delete" title="Delete">×</span></div>
        </div>
    </div>
</t>
</templates>
